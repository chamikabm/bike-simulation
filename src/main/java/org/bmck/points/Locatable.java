package org.bmck.points;

/**
 * Interface for a locatable object.
 */
public interface Locatable {
    /**
     * Retrieves a printable representation of the object's location.
     *
     * @return The printable location as a string.
     */
    String getPrintableLocation();
}
